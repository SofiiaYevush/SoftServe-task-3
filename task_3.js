// ------------------- 1 --------------------

function createArray(start, end)
{
  let i = start;
  while (i <= end)
  {
    console.log(i);
    ++i;
  }
}
let arr = createArray(2, 9);
console.log(arr);